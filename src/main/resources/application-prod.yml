logging:
  level:
    root: INFO
    com.wex.payments: INFO

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

treasury:
  api:
    base-url: https://api.fiscaldata.treasury.gov
    connect-timeout: 2s
    read-timeout: 5s
    max-attempts: 3
